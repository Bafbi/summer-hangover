import { Expense } from "./expense-card";

export function Balance({
    expense,
    isFavorite,
  }: {
    expense: Expense;
    isFavorite: boolean;
  }) {

    // Calculate balance between all users
    
}